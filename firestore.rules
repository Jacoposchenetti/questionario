rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /responses/{doc} {
      allow write: if true;
      allow read: if request.auth != null;
    }
  }
}
